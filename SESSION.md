# Session Notes — Resume From Here

> If Claude Code was closed or crashed, read this file first before doing anything.
> Update this file at the end of every working session.

---

## Last Updated
2026-03-20

## Active Branch
`feature/claude-skills` (branched from `main`)

## Current Goal
Add a **Claude Skills** section to AIFoxx (aifox.com):
- A standalone `/skills` page listing GitHub repos for MCP servers + Claude Code skills
- A "Claude Skills" tab/section on each tool's detail page (`/ai/:slug`)
- A GitHub scraper script that auto-populates `src/data/skills.json`

---

## What's Been Done

### IDEA Generator Repo (`D:\Coding and FUN\IDEA Generator\`)
- [x] Created `ideas.md` — raw brain dump of all 5 project ideas
- [x] Created `README.md` — master dashboard with prioritization table
- [x] Created per-idea folders with `idea.md` + `execution/notes.md` for each

### AIFoxx Project (`D:\Coding and FUN\AIFoxx\aifoxx\`)
- [x] Explored full project structure — React 18 + Vite + React Router + static JSON
- [x] Created `CLAUDE.md` — architecture, versioning, security, execution plan, testing, GitHub best practices
- [x] Created `SESSION.md` (this file)
- [x] Created branch `feature/claude-skills` from `main`

---

## What Needs to Be Done Next

Work through these in order:

### Step 1 — Skill Type Definition
- [x] Created `src/types/skill.ts` — Skill type + Zod schema

### Step 2 — GitHub Scraper Script
- [x] Created `scripts/scrape-github-skills.mjs`
- [x] Ran scraper — 239 skills written to `src/data/skills.json`
- [ ] Store `GITHUB_TOKEN` in `.env.local` for future runs (token was shared in chat — regenerate it)

### Step 3 — Skills Data Library
- [x] Created `src/lib/skills.ts` — `allSkills`, `getSkillsByToolSlug()`, `searchSkills()`, `SKILL_COUNTS`

### Step 4 — Skills Browse Page
- [x] Created `src/pages/SkillsPage.tsx` — `/skills` route with search + type filter + pagination
- [x] Added `/skills` route in `src/App.tsx`
- [x] Added "SKILLS" link to `NavBar.tsx`
- [x] `npm run build` passes clean

### Step 5 — Tool Detail Page Integration
- [ ] Edit `src/pages/ToolDetailPage.tsx`
  - Import `getSkillsByToolSlug` from `src/lib/skills.ts`
  - Add "Claude Skills" section after existing tool info
  - Only render if `skills.length > 0`

### Step 6 — Polish & Deploy
- [ ] Test on mobile viewport
- [ ] Commit and push `feature/claude-skills` branch
- [ ] Create PR → merge to main → Vercel auto-deploys

---

## Key Files to Know

| File | Purpose |
|------|---------|
| `src/App.tsx` | Route config — add `/skills` route here |
| `src/pages/ToolDetailPage.tsx` | Tool detail — add Claude Skills section here |
| `src/pages/HomePage.tsx` | Tool listing (reference for page layout patterns) |
| `src/lib/tools.ts` | Mirror this pattern for `src/lib/skills.ts` |
| `src/types/tool.ts` | Mirror this for `src/types/skill.ts` |
| `src/data/tools.json` | 3.7MB tools data — DO NOT edit manually |
| `src/data/skills.json` | Skills data — generated by scraper (doesn't exist yet) |
| `CLAUDE.md` | Full project guide — read this too |

---

## Important Context

- **No backend** — pure static SPA, all data is JSON files loaded at build time
- **No DB** — skills.json is the "database", regenerated by running the scraper
- **Deployed on Vercel** — `main` branch auto-deploys
- **Shadcn components** at `src/components/ui/` — DO NOT edit these manually
- **Skill types in scope**: MCP servers + Claude Code skills (not Claude.ai tools)
- **GitHub token needed** for scraper — store in `.env.local` as `GITHUB_TOKEN`

---

## Questions / Decisions Pending

- [ ] Does the user have a GitHub API token? (Needed for scraper — 5000 req/hr vs 60 without)
- [ ] Should the `/skills` page be added to the main navigation bar?
- [ ] Should skills be searchable from the existing homepage search?
